#include "main.h"

/**
 * mul - Multiplies two intergers
 * @x: The first number to be multiplied
 * @y: The secone number to multiply with first number
 * Return: the result of multiplication between x param and y param
 */

int mul(int x, int y)
{
	return (x * Y);
}
